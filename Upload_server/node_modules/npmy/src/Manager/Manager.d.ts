import Package from '../Package/Package';
import ObservablePackage from '../ObservablePackage/ObservablePackage';
export declare type RCScanResult = {
    path: string;
    rc: object;
};
export default class Manager {
    items: RCScanResult[];
    itemsIndex: {
        [path: string]: RCScanResult;
    };
    observables: {
        [path: string]: boolean;
    };
    packages: {
        [path: string]: Package;
    };
    observablePackages: {
        [path: string]: ObservablePackage;
    };
    private preparePackage(path, notObservable, initialRC);
    run(): Promise<void>;
    scan(cwd: string, include?: string): Promise<RCScanResult[]>;
    private addItem(path, rc);
    private verbose(msg);
}
