import Package, { INPMyrc } from '../Package/Package';
export default class ObservablePackage extends Package {
    path: string;
    npmy: INPMyrc;
    private processing;
    private taskRetries;
    private rsyncGhostPath;
    private ghostPath;
    private tasks;
    private gitignore;
    constructor(path: string, npmy: INPMyrc);
    expire(rsync: any): void;
    addTask(exec: any): void;
    simulatePublish(): void;
    protected getPathToPublished(): string;
    protected runInstall(createBinScripts: boolean): Promise<void>;
    private readGitignore();
    private startWatcher();
    private rsyncGhost(excludeNodeModules);
    cleanGhostAfterPublish(): Promise<void>;
    runNextTask(): Promise<void>;
}
